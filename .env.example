# Client-side app mode. Keep this as mock until a server-side AI endpoint is ready.
VITE_AI_MODE=mock

# Optional backend endpoint for a future Vercel/Netlify/serverless function.
VITE_AI_ENDPOINT=/api/evaluate

# Server-side only. Do not expose this with a VITE_ prefix.
AI_PROVIDER_API_KEY=
